# frontend-nzila-plus
frontend de um projeto da PAP

## Nzila Plus — Expo (TypeScript)

This repository contains an Expo SDK 50 React Native application (TypeScript) generated from an OpenAPI specification. It is scaffolded to run in Codespaces or locally and supports EAS development builds.

See `QUICKSTART.md` for exact local development and build commands.
